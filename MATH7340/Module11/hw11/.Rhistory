data(golub, package="multtest")
oncoIndex <- grep("oncogene", golub.gnames[,2])
antiIndex <- grep("antigen", golub.gnames[,2])
oncoIndex
antiIndex
golub[501,]
iris$Species
gol.fac <- factor(golub.cl,levels=0:1, labels= c("ALL","AML"))
gol.fac
golub[oncoIndex,]
dim(golub[oncoIndex,])
dim(golub[antiIndex,])
data(golub, package="multtest")
oncoIndex <- grep("oncogene", golub.gnames[,2])
antiIndex <- grep("antigen", golub.gnames[,2])
clusdata <- rbind(golub[ondoIndex,], golub[antiIndex,])
data(golub, package="multtest")
oncoIndex <- grep("oncogene", golub.gnames[,2])
antiIndex <- grep("antigen", golub.gnames[,2])
clusdata <- rbind(golub[oncoIndex,], golub[antiIndex,])
dim(clusdata)
data(golub, package="multtest")
tag <- c("oncogene", 42) + c("antigen", 75)
data(golub, package="multtest")
tag <- c("oncogene", 42) #c("antigen", 75)
oncoIndex <- grep("oncogene", golub.gnames[,2])
antiIndex <- grep("antigen", golub.gnames[,2])
clusdata <- rbind(golub[oncoIndex,], golub[antiIndex,])
tag
data(golub, package="multtest")
tag <- rep("oncogene", 42) #c("antigen", 75)
oncoIndex <- grep("oncogene", golub.gnames[,2])
antiIndex <- grep("antigen", golub.gnames[,2])
clusdata <- rbind(golub[oncoIndex,], golub[antiIndex,])
tag
data(golub, package="multtest")
tag <- rep("oncogene", 42) + rep("antigen", 75)
data(golub, package="multtest")
tag <- c(rep("oncogene", 42), rep("antigen", 75))
oncoIndex <- grep("oncogene", golub.gnames[,2])
antiIndex <- grep("antigen", golub.gnames[,2])
clusdata <- rbind(golub[oncoIndex,], golub[antiIndex,])
tag
data(golub, package="multtest")
tag <- c(rep("oncogene", 42), rep("antigen", 75))
oncoIndex <- grep("oncogene", golub.gnames[,2])
antiIndex <- grep("antigen", golub.gnames[,2])
clusdata <- rbind(golub[oncoIndex,], golub[antiIndex,])
gol.fac <- factor(tag,levels=0:1, labels= c("oncogene","antigen"))
gol.fac
tag
data(golub, package="multtest")
tag <- c(rep(0, 42), rep(1, 75))
oncoIndex <- grep("oncogene", golub.gnames[,2])
antiIndex <- grep("antigen", golub.gnames[,2])
clusdata <- rbind(golub[oncoIndex,], golub[antiIndex,])
gol.fac <- factor(tag,levels=0:1, labels= c("oncogene","antigen"))
gol.fac
cl.2mean <- kmeans(clusdata, centers=2, nstart=10)
cl.2mean
cl.2mean <- kmeans(clusdata, centers=2, nstart=10)
table(gol.fac, cl.2mean$cluster)
cl.2mean <- kmeans(clusdata, centers=2, nstart=10)
table(gol.fac, cl.2mean$cluster)
cl.2medoid <- pam(dist(clusdata, method='eucl'), k=2)
library(cluster)
cl.2mean <- kmeans(clusdata, centers=2, nstart=10)
table(gol.fac, cl.2mean$cluster)
cl.2medoid <- pam(dist(clusdata, method='eucl'), k=2)
cl.2mean <- kmeans(clusdata, centers=2, nstart=10)
table(gol.fac, cl.2mean$cluster)
cl.2medoid <- pam(dist(clusdata, method='eucl'), k=2)
table(gol.fac, cl.2medoid$cluster)
hc.single <- hclust(dist(clusdata, method="euclidian"), method="single")
plot(hc.single, labels=gol.fac)
rect.hclust(hc.single, k=2)
hc.complete <- hclust(dist(clusdata, method="euclidian"), method="complete")
plot(hc.complete, labels=gol.fac)
rect.hclust(hc.single, k=2)
hc.single <- hclust(dist(clusdata, method="euclidian"), method="single")
plot(hc.single, labels=gol.fac)
rect.hclust(hc.single, k=2)
hc.complete <- hclust(dist(clusdata, method="euclidian"), method="complete")
plot(hc.complete, labels=gol.fac)
rect.hclust(hc.complete, k=2)
hc.single <- hclust(dist(clusdata, method="euclidian"), method="single")
plot(hc.single)
rect.hclust(hc.single, k=2)
hc.complete <- hclust(dist(clusdata, method="euclidian"), method="complete")
plot(hc.complete)
rect.hclust(hc.complete, k=2)
hc.single <- hclust(dist(clusdata, method="euclidian"), method="single")
plot(hc.single, labels=gol.fac, cex=0.5)
rect.hclust(hc.single, k=2)
hc.complete <- hclust(dist(clusdata, method="euclidian"), method="complete")
plot(hc.complete, labels=gol.fac)
rect.hclust(hc.complete, k=2)
hc.single <- hclust(dist(clusdata, method="euclidian"), method="single")
plot(hc.single, labels=gol.fac, cex=0.35)
rect.hclust(hc.single, k=2)
hc.complete <- hclust(dist(clusdata, method="euclidian"), method="complete")
plot(hc.complete, labels=gol.fac)
rect.hclust(hc.complete, k=2)
library(cluster)
cl.2mean <- kmeans(clusdata, centers=2, nstart=10)
meanTable <- table(gol.fac, cl.2mean$cluster)
meanTable
cl.2medoid <- pam(dist(clusdata, method='eucl'), k=2)
medoidTable <- table(gol.fac, cl.2medoid$cluster)
medoidTable
fisher.test(meanTable)
fisher.teset(medoidTable)
fisher.test(meanTable)
fisher.test(medoidTable)
cl.2mean <- kmeans(clusdata, centers=2, nstart=10)
meanTable <- table(gol.fac, cl.2mean$cluster)
meanTable
cl.2medoid <- pam(dist(clusdata, method='eucl'), k=2)
medoidTable <- table(gol.fac, cl.2medoid$cluster)
medoidTable
cl.2mean <- kmeans(clusdata, centers=2, nstart=10)
meanTable <- table(gol.fac, cl.2mean$cluster)
meanTable
cl.2medoid <- pam(dist(clusdata, method='eucl'), k=2)
medoidTable <- table(gol.fac, cl.2medoid$cluster)
medoidTable
fisher.test(meanTable)
fisher.test(medoidTable)
initial
